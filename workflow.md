We start from repo:

for each US a new branch is built using the code defined by the PO
example:

```
US-E1.1
```

execute the updates on US repo.
test and verify that all is correctly working and all the expected outcome are properly executed.

Before creating a pull request
merge again the main to your local branch and resolve the conflict.

create a pull request
